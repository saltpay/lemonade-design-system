import 'package:flutter/animation.dart';

/// Duration for general animations in the Lemonade Design System.
const Duration kAnimationDuration = Duration(milliseconds: 150);

/// Default animation curve for smooth transitions.
const Curve kAnimationCurve = Curves.easeInOut;

/// Default focus ring offset (positive = outward, negative = inward).
const double kFocusRingOffset = 2;
