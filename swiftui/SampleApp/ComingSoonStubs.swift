import SwiftUI

// Stubs for components not yet implemented in this version of Lemonade
struct MarkdownDisplayView: View {
    var body: some View { Text("Coming soon").foregroundColor(.gray).padding() }
}

struct InlineCalendarDisplayView: View {
    var body: some View { Text("Coming soon").foregroundColor(.gray).padding() }
}

struct HistoryTimelineDisplayView: View {
    var body: some View { Text("Coming soon").foregroundColor(.gray).padding() }
}
